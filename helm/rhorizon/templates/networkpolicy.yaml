{{- if .Values.networkPolicy.enabled -}}
# API : ingress from frontend + (optionally) external CIDRs ; egress to PG + DNS only.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-api" (include "rhorizon.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rhorizon.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "rhorizon.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: api
  policyTypes:
    - Ingress
    - Egress
  ingress:
    {{- if .Values.frontend.enabled }}
    # Frontend can reach the API.
    - from:
        - podSelector:
            matchLabels:
              {{- include "rhorizon.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: frontend
      ports:
        - port: 8200
          protocol: TCP
    {{- end }}
    # Same-namespace pods (e.g. rh-fetch / rh-watch sidecars) can reach
    # the API directly. Cross-namespace clients must add their own rule.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Release.Namespace }}
      ports:
        - port: 8200
          protocol: TCP
  egress:
    {{- if and .Values.api.clusterEnabled .Values.frontend.enabled }}
    # HA JOIN, renewal and live preflight traverse the same HTTPS/mTLS
    # frontend path as production traffic.
    - to:
        - podSelector:
            matchLabels:
              {{- include "rhorizon.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: frontend
      ports:
        - port: {{ .Values.service.frontend.httpsPort }}
          protocol: TCP
    {{- end }}
    # PostgreSQL.
    {{- if not .Values.postgres.external.enabled }}
    - to:
        - podSelector:
            matchLabels:
              {{- include "rhorizon.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: postgres
      ports:
        - port: 5432
          protocol: TCP
    {{- else }}
    # External PG (Patroni / managed): allow egress to the DB port. Tighten the
    # destination with postgres.external.egressCidrs; otherwise cluster-wide but
    # port-scoped, which covers an in-cluster Patroni without its pod IP.
    - to:
        {{- if .Values.postgres.external.egressCidrs }}
        {{- range .Values.postgres.external.egressCidrs }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
        {{- else }}
        - namespaceSelector: {}
        {{- end }}
      ports:
        - port: {{ .Values.postgres.external.port }}
          protocol: TCP
        {{- if .Values.postgres.external.patroniRestUrls }}
        # The HA tier is polled over Patroni's REST API, not the SQL port.
        # Declaring patroniRestUrls without opening this makes the poll fail
        # and cluster health report database_ha red on a healthy cluster.
        - port: {{ .Values.postgres.external.patroniRestPort }}
          protocol: TCP
        {{- end }}
    {{- end }}
    # DNS.
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
{{- if .Values.frontend.enabled }}
---
# Frontend : ingress from anywhere (or restricted via ingressFrontendCidrs) ; egress to API only.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ printf "%s-frontend" (include "rhorizon.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rhorizon.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "rhorizon.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: frontend
  policyTypes:
    - Ingress
    - Egress
  ingress:
    {{- if .Values.api.clusterEnabled }}
    # API pods must reach the frontend for JOIN, renewal and live preflight,
    # independently of the external client allow-list below.
    - from:
        - podSelector:
            matchLabels:
              {{- include "rhorizon.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: api
      ports:
        - port: {{ .Values.service.frontend.httpsPort }}
          protocol: TCP
    {{- end }}
    {{- if .Values.networkPolicy.ingressFrontendCidrs }}
    - from:
        {{- range .Values.networkPolicy.ingressFrontendCidrs }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        - port: 8200
          protocol: TCP
        {{- if or .Values.frontend.tls.enabled .Values.api.clusterEnabled }}
        - port: {{ .Values.service.frontend.httpsPort }}
          protocol: TCP
        {{- end }}
    {{- else }}
    # No CIDR restriction declared, accept from any pod or ingress
    # controller in the cluster. Lock down via .Values.networkPolicy.ingressFrontendCidrs.
    - {}
    {{- end }}
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "rhorizon.selectorLabels" . | nindent 14 }}
              app.kubernetes.io/component: api
      ports:
        - port: 8200
          protocol: TCP
    - to:
        - namespaceSelector: {}
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
{{- end }}
{{- end -}}
